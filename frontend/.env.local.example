# Copy to .env.local (gitignored by the Next.js scaffold's default .gitignore)
# for local development.

# Base URL of the FastAPI backend (see backend/.env's default host:port,
# or backend/Dockerfile / docker-compose.yml when running containerized).
NEXT_PUBLIC_API_URL=http://localhost:8000
