# Copy to backend/.env for local (non-Docker) development.
# Not used yet by Phase 0 — placeholder for settings later phases will add
# (redis, postgres, jwt secret, llm api keys). Only APP_NAME/ENVIRONMENT/
# LOG_LEVEL are read today.

APP_NAME=prompt-security-framework
ENVIRONMENT=development
LOG_LEVEL=INFO
