# --- API / settings ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.13.4
pydantic-settings==2.15.0

# --- data layer (context buffer, persistence, explainable logging) ---
redis==5.0.8
asyncpg==0.29.0
sqlmodel==0.0.22
alembic==1.13.2

# --- config/rule files (swcsa, policy) ---
pyyaml==6.0.2

# --- NLP / drift / dedup (SWCSA, preprocessing, IFS-R) ---
# numpy pinned <2.0: spaCy's thinc backend (compiled C extensions) is
# ABI-incompatible with numpy 2.x as of thinc 8.2.4, and pip's resolver
# doesn't catch this — it's a runtime crash, not an install-time conflict.
numpy==1.26.4
spacy==3.7.5
en-core-web-sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl
sentence-transformers==3.1.1
datasketch==1.6.5

# --- multi-modal input (audio/image) ---
faster-whisper==1.0.3
pytesseract==0.3.13
pillow==10.4.0
scipy==1.13.1

# --- target LLM (llm_gateway phase) ---
anthropic==1.0.0
google-genai==2.19.0

# --- auth / RBAC ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# bcrypt pinned <4.1: passlib 1.7.4 (unmaintained since 2020) probes
# bcrypt's `__about__.__version__` attribute during backend detection,
# which bcrypt 4.1+ removed — pip's resolver doesn't catch this, it's a
# runtime crash on the first hash() call, not an install-time conflict
# (same class of issue as the numpy/thinc pin above).
bcrypt==4.0.1
python-multipart==0.0.9

# --- dev / test / lint ---
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.28.1
ruff==0.6.9
mypy==1.11.2
fakeredis==2.24.1
aiosqlite==0.20.0
